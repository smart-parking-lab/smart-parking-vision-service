# Web framework (bỏ [standard] để tránh kéo email-validator, jinja2, itsdangerous)
fastapi==0.115.12
uvicorn==0.34.3

# Database (vision service dùng session store local)
sqlalchemy==2.0.41
psycopg2-binary==2.9.10

# HTTP client
httpx==0.28.1

# Computer Vision — headless: không cần Qt/GUI libs, nhẹ hơn ~30MB
opencv-python-headless==4.11.0.86
numpy==1.26.4
Pillow==11.2.1

# OCR engine — pin chặt để tránh resolve conflict khi install
paddlepaddle==2.6.2
paddleocr==2.7.3

# Utils
python-dotenv==1.1.0
cloudinary==1.42.1
setuptools==80.9.0